/**
 * Represents a state.
 * This class encapsulates information about a state, including its ID and name.
 */
export class State {
    /** The unique identifier for the state. */
    id!: number;
    /** The name of the state. */
    name!: string;
}
